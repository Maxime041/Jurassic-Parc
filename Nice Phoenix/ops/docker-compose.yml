version: '3.8'

services:
  api:
    build:
      context: ..
      dockerfile: ops/Dockerfile

    ports:
      - "3003:3003"
    environment:
      - NODE_ENV=production
      - PORT=3003
      - MONGODB_URI=mongodb://mongodb:27017/nice-massena
    volumes:
      - ../:/app
      - /app/node_modules

  mongodb:
    image: mongo:7
    environment:
      MONGO_INITDB_DATABASE: nice-massena
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data: