name: API Parc 1 - Nice Massena

on:
  push:
    branches: [ main, dev, staging ]
    paths:
      - 'Nice Massena/**'
  pull_request:
    branches: [ main, dev, staging ]
    paths:
      - 'Nice Massena/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: cd "Nice Massena" && npm install

      - name: Run tests
        run: cd "Nice Massena" && npm test

      - name: Build Docker image
        run: cd "Nice Massena" && docker build -t nice-massena-api -f ops/Dockerfile .